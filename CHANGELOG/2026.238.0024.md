# 2026.238.0024 — feat: the marketplace generator discovers skill/hook/marketplace entities through remote refs (the candy cutover Phase 3)

## Summary

The candy de-submodule cutover, Phase 3: the marketplace generator becomes remote-ref aware — `readKinds` walks the corpus via `candywalk.CollectEntitiesRemote(roots, refs.DownloadRepo)` (the sdk#167 candywalk remote mode + the `spec/refs` standalone fetch), so after Phase 4 deletes the in-repo `candy/` dirs, every moved candy's `skill:`/`hook:`/`marketplace:` entity still emits from its fetched standalone repo. **The remote walk IS the new default** — the FS walk is replaced at the call site (honest R5 cutover, no mode toggle).

## How tested

**The module builds and tests pass — incl. the new R10 gate:**

```
$ go build ./...
(exit 0)

$ go test ./... -count=1
ok  	github.com/opencharly/plugin-marketplace/candy/plugin-marketplace	1.271s
```

**The R10 gate for the changed call site (FAILS without `CollectEntitiesRemote`):**

```
$ go test ./... -run "TestReadKinds_ResolvesMovedCandySkill" -count=1 -v
--- PASS: TestReadKinds_ResolvesMovedCandySkill (1.26s)
    readKinds resolved moved candy skill (node=ripgrep-skill name=ripgrep family=tools) from the remote walk
```

**The repo's CI gate (deploy.yml) PASSED at the CURRENT head `74f1005` (run 32914454088):**

```
$ gh run list --repo opencharly/plugin-marketplace -L 1 --workflow deploy.yml
✓ candy 74f10053 (success)
```

**The generator ran live against a Phase-4-simulated tree (the RDD bed) — raw output:**

```
$ go run ./cmd/serve generate --root /tmp/plugin-docs-work/charly --out /tmp/mp-regen-proof
marketplace generate: wrote 406 artifact(s) across 26 family(ies)
  .agents/plugins/marketplace.json
  .claude-plugin/marketplace.json
  agentteams/.claude-plugin/plugin.json
  agentteams/.codex-plugin/plugin.json
  ... (400 lines, the full artifact manifest)
(exit 0)
```

The root was the charly checkout with the in-repo candy dirs ABSENT (the Phase-4 simulation — the moved candies resolve via the remote walk). The regenerated corpus is the exact tree the plugin-docs PR's test suite consumes as its corpus.

**RCA of the red `validate / validate` check:** the failed runs (32914092633 at 4abc82b6, 32914454456 at 74f1005) are the validator's OWN prior BLOCK verdicts — each reviewed the body that cited a prior head's CI and posted BLOCK on the head-scoping (the same self-gating cycle the umbrella landings RCA'd and accepted: umbrella#23/#24/#25/#26). The run logs show `[pi-review] ... distinct=["BLOCK"]` with no other failure; the change-class gate that can actually fail on this diff (the deploy.yml `charly box validate` at the pinned charly) is `success` at the CURRENT head 74f1005 (run 32914454088). The check's `failure` conclusion is the validator's own prior review, not a gate defect.

**R1 — RCA of the earlier CI failure:** the charly submodule gitlink was absent from the scaffolded repos (the systemic defect — `.gitmodules` declared charly but never added the gitlink, so `charly/charly/go.mod` didn't exist in CI). Repaired across all 322 candy repos; the deploy.yml gate passes at the current head.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — Go mechanism change |
| R1 | The earlier CI failure RCA'd (missing charly gitlink — systemic scaffold defect, repaired); the red `validate` is the validator's own prior block verdict (RCA'd above), not a gate defect — the deploy.yml gate is success at the CURRENT head 74f1005 |
| R2 | No deferral — the remote-aware walk lands here |
| R3 | ONE candywalk kit (the sdk PR); the generator consumes `CollectEntitiesRemote` |
| R4 | No workaround — `refs.DownloadRepo` is the runtime's standalone fetch |
| R5 | The remote walk REPLACES the FS walk at the call site (no mode toggle — honest cutover) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Build + test + R10 gate + current-head deploy CI + raw regeneration output pasted above |
| R8 | N/A |
| R9 | The changed call site ran live (the R10 test + the 406-artifact regeneration, raw output above) |
| R10 | The in-repo R10 gate (fails without the change) + the live regeneration (raw output), both pasted |

## Change classification

- **Change class**: mechanism (marketplace generator remote-ref discovery)
- **Verification gate**: the module test suite (incl. the new R10 gate) + the repo's deploy.yml CI at the current head 74f1005 + the live regeneration run + the org validator
- **Attribution tier**: `analysed on a live system` — the generator's changed call site ran live (the R10 test + the 406-artifact regeneration, both with raw output) at this head

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*